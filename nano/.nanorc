# .nanorc
# initialization file for GNU nano

# attempt to display the entire contents of a line if wider than screen width by wrapping to the next
set softwrap

# scroll text line-by-line instead of chunk-by-chunk
set smooth
